import { Sidebar } from "react-pro-sidebar";

// TODO: Implement this component
// Displays the image info in the sidebar if image info is clicked on from the navbar
const ImageInfo = () => {

    return (
        <Sidebar>
            Image Info
        </Sidebar>
    );
}

export default ImageInfo;